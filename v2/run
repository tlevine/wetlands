#!/bin/sh

set -e
. ./papers.sh

# Save the listing page.
python2 listing.py

# Save unprocessed pdf files.
runpapers=$(mktemp)
python2 get_unprocessed.py > $runpapers
sh $runpapers

# translate "${file}"
# cat "${file}" > 
